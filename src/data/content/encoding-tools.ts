import type { ToolContent } from './types';

const shared = ['base64-tool', 'url-encoder', 'base-converter', 'json-formatter'];
export const encodingInspectorContent: ToolContent = { slug: 'encoding-inspector', intro: ['Encoding Inspector checks a value against several common text representations: Base64, Base64URL, hexadecimal bytes, URL percent-encoding, HTML entities, Unicode escapes, JWT-shaped values, and 8-bit binary. It reports possible matches rather than pretending that detection is certain.', 'Use the explicit decode buttons when you want to apply a known transformation one step at a time. The input and results stay in your browser.'], sections: [{ heading: 'Why detection is necessarily heuristic', paragraphs: ['Encodings do not always identify themselves. A short string can be valid plain text, Base64, hexadecimal, or several of these at once. A hash, identifier, compressed payload, or encrypted value can also look like an encoding.', 'The inspector therefore shows evidence such as valid alphabets, successful decoding, UTF-8 validity, and printable output. Treat the result as a lead for investigation, not proof of origin.'] }, { heading: 'Use explicit steps for layered values', paragraphs: ['Real values are often wrapped more than once: a URL parameter may contain Base64, or a JSON string may contain Unicode escapes. Choose one decoder at a time and inspect the result before continuing.', 'The tool does not recursively decode automatically. That avoids silently changing data or turning an ambiguous input into a misleading answer.'], tip: 'For JWTs, a recognised three-part shape only describes the structure. It does not verify the signature, issuer, audience, or expiry.' }, { heading: 'Encoding is not security', paragraphs: ['Base64, hexadecimal, URL escapes, HTML entities, and Unicode escapes are reversible representations. They do not provide confidentiality. Encrypted and compressed data may be impossible to preview as UTF-8 even when the bytes are valid.'] }], faqs: [{ q: 'Can the inspector always identify an encoding?', a: 'No. Detection is heuristic because many formats overlap and short strings provide little evidence.' }, { q: 'Does a JWT-shaped value mean the token is valid?', a: 'No. It only has three Base64URL-looking segments. Signature and claim validation must happen in the application with a trusted library and policy.' }, { q: 'Why does a hash appear as possible hex?', a: 'Hashes are commonly printed as hexadecimal, so the character pattern is valid. That does not mean the digest can be decoded back to the original input.' }], relatedSlugs: shared };
export const hexToolContent: ToolContent = { slug: 'hex-encoder', intro: ['Hexadecimal, or Base16, represents each byte with two characters from 00 to FF. This tool converts UTF-8 text to hex and decodes pairs of hex digits back into UTF-8 text, with optional spaces, colons, or hyphens between bytes.', 'Hex is a representation of bytes, not automatically readable text. Hashes, compressed data, and encrypted bytes may be valid hex but should not be treated as text.'], sections: [{ heading: 'Text becomes UTF-8 bytes', paragraphs: ['ASCII characters occupy one UTF-8 byte, while characters such as é and emoji occupy multiple bytes. The encoder shows those actual bytes, so the output length depends on UTF-8 rather than the number of visible characters.'] }, { heading: 'Hex is common, but context matters', list: ['Debugging byte sequences and network payloads.', 'Displaying hashes and checksums in a compact form.', 'Representing colour channels, memory values, and binary file data.', 'Inspecting Unicode and protocol data when the byte boundaries matter.'] }, { heading: 'Valid hex is not necessarily decodable text', paragraphs: ['The decoder requires complete byte pairs and strict hexadecimal digits, then attempts UTF-8 decoding. If the bytes are not valid UTF-8, that is useful information: the value may be binary data or another encoding.'] }], faqs: [{ q: 'How many hex digits represent one byte?', a: 'Two. Each hexadecimal digit represents four bits, so two digits represent eight bits (one byte).' }, { q: 'Can I paste spaced hex?', a: 'Yes. Spaces, colons, and hyphens are ignored between byte pairs, but the remaining digits must be valid and have an even length.' }, { q: 'Is hex encryption?', a: 'No. Hex is a reversible representation of bytes and provides no confidentiality.' }], relatedSlugs: ['base-converter', 'base64-tool', 'hash-generator'] };
export const unicodeEscapeToolContent: ToolContent = { slug: 'unicode-escape-tool', intro: ['Unicode escapes represent characters using code points, commonly in JavaScript and JSON strings. This tool converts text to readable JavaScript-style escapes and decodes four-digit and brace-style escapes back into text.', 'Escaping changes how text is written; it does not encrypt or compress the text.'], sections: [{ heading: 'Code points and UTF-16', paragraphs: ['Basic Multilingual Plane characters can be written as four hexadecimal digits such as \\u00e9. Characters outside that range, including many emoji, need a supplementary code point; this tool uses the modern brace form such as \\u{1f600} for clarity.'] }, { heading: 'JSON compatibility', paragraphs: ['JSON commonly uses four-digit Unicode escapes and surrogate pairs for supplementary characters. The brace form is convenient JavaScript syntax but is not accepted by every JSON parser, so use the four-digit form or a correctly generated surrogate pair when strict JSON compatibility is required.'] }, { heading: 'Use escaping for the right layer', paragraphs: ['Escape text when a file format or programming language requires it. Do not use it as a security boundary, and do not confuse a displayed escape sequence with the underlying character value.'] }], faqs: [{ q: 'Is a Unicode escape a different character?', a: 'Usually no. It is another written representation of the same Unicode code point.' }, { q: 'Why do emoji use more than four digits?', a: 'Many emoji are outside the Basic Multilingual Plane and need a supplementary code point.' }, { q: 'Are brace-style escapes valid JSON?', a: 'Not universally. Strict JSON uses four-digit escapes, with surrogate pairs for supplementary characters.' }], relatedSlugs: ['json-formatter', 'base-converter', 'regex-tester'] };
export const htmlEntityToolContent: ToolContent = { slug: 'html-entity-tool', intro: ['HTML entities are character references such as &amp; and &lt; that let HTML represent reserved characters. This tool encodes the five most important HTML-sensitive characters and decodes common named, decimal, and hexadecimal references.', 'It is a text conversion aid, not an HTML sanitizer. Never assume that encoding or decoding alone makes untrusted markup safe to insert into every context.'], sections: [{ heading: 'Why entities exist', paragraphs: ['The characters < and > have meaning in HTML markup, while & starts a character reference. Quoting them as &lt;, &gt;, and &amp; lets them appear as text instead of being interpreted as tags or references. Quotes are also commonly escaped inside attribute values.'] }, { heading: 'Named and numeric references', paragraphs: ['HTML supports named references such as &quot; and &nbsp;, plus decimal references such as &#39; and hexadecimal references such as &#x27;. Numeric forms are useful when a named entity is unavailable or when preserving a code point explicitly.'] }, { heading: 'Encoding is context-specific', paragraphs: ['HTML text, HTML attributes, URLs, JavaScript strings, and CSS each have different escaping rules. Use the encoder for HTML text and attribute values, then apply the escaping rules for the actual context in which the data will be inserted.'] }], faqs: [{ q: 'Does this sanitize HTML?', a: 'No. It converts common character references only. Sanitizing untrusted HTML requires a context-aware, maintained sanitizer and safe rendering strategy.' }, { q: 'What does &amp; represent?', a: 'It represents the ampersand character (&), which otherwise begins an HTML character reference.' }, { q: 'Should I HTML-encode a URL?', a: 'Use URL percent-encoding for URL components. HTML-encode only when placing that URL inside HTML markup.' }], relatedSlugs: ['html-formatter', 'url-encoder', 'markdown-to-html'] };
